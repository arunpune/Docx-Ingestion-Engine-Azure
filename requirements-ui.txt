# UI Application Requirements
fastapi==0.109.0
uvicorn==0.27.0
jinja2==3.1.2
python-multipart==0.0.6
aiofiles==23.2.1

# Email processing
# Will be installed dynamically by the UI

# Azure (will be installed by prerequisites setup)
# azure-storage-blob
# azure-identity

# OCR (will be installed by prerequisites setup)  
# PyPDF2
# Pillow
# pytesseract

# AI (optional)
# openai
